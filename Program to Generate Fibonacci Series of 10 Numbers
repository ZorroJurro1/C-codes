#include <stdio.h> 
int main() {
int i, n = 10
; unsigned long long a = 0, b = 1, c; 
printf("Fibonacci series of first 10 numbers:\n"); 
printf("%llu %llu ", a, b); 
for (i = 2; i < n; i++) { 
c = a + b; printf("%llu ", c); 
a = b; b = c; } 
printf("\n"); 
return 0; 
}
